name: MAIL
on:
  schedule:
    - cron: "5 4 * * *"
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: superman510/mail@v1
    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with:
        python-version: 3.9
    - name: Install requirements
      run: |
        python -m pip install --upgrade pip
        pip install requests
        pip install smtplib
    - name: SignIn
      run: |
        python ./mail.py
